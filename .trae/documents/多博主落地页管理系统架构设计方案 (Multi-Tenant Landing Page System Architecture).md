根据您的确认，我将立即开始执行代码改造。

**执行步骤：**
1.  **安装依赖**：安装 `react-router-dom` 以支持多页面路由。
2.  **重构数据**：将 `data.json` 改造为支持多博主的结构（模拟数据库）。
3.  **页面拆分**：
    *   创建 `PublicProfile.jsx`：用于动态展示不同博主的落地页（如 `/firestick`, `/tech-review`）。
    *   创建 `AdminDashboard.jsx`：用于后续的后台管理。
4.  **路由配置**：修改 `App.jsx`，实现 URL 到对应博主页面的映射。

完成后，您可以通过不同的 URL 访问不同的博主页面。